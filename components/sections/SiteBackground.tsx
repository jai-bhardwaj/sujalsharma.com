// Each section now paints its own background — this file exists only so
// importing SiteBackground keeps working without breaking the layout.
export default function SiteBackground() {
  return null
}
